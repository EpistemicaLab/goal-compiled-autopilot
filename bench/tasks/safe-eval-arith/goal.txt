write safearith.py exposing a function eval_arith(s: str) -> int that safely evaluates an arithmetic expression over integers using +, -, *, /, parentheses, and unary minus, and raises ValueError on anything else (attribute access, function calls, names, imports, statements). Use only python stdlib. Also write test_safearith.py demonstrating both allowed expressions and rejected attack strings.
