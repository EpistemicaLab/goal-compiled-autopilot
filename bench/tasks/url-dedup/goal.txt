write urldedup.py exposing a function dedup(urls: list[str]) -> list[str] that deduplicates URLs treating these as equivalent: case-insensitive scheme (http vs https are NOT equivalent, but HTTP and http ARE), case-insensitive host, an optional trailing slash on the path, and a URL fragment (#...) which should be ignored. Preserve the order of first occurrence in the input. Use only python stdlib. Also write test_urldedup.py.
